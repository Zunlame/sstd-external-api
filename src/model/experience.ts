export class Experience {

    role: string;
    company: string;
    duration: string;

    constructor(role: string, company: string, duration: string) {
        this.role = role;
        this.company = company;
        this.duration = duration;
    }

}